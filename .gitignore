# Claude Code memory & settings — never push these
.claude/

# OS junk
.DS_Store
Thumbs.db

# Environment / secrets
.env
*.env

# Local media tooling (ffmpeg, scratch clips used to build the title-screen loops)
.tools/
